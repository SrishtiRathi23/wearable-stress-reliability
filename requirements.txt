# Minimal environment for the bootstrap + upcoming data-audit stage.
# Exact resolved versions are recorded in requirements-lock.txt (generated with
# `uv pip freeze`) so a result can be tied to a package set.
#
# Install:  uv venv --python 3.12 .venv && uv pip install -r requirements.txt -e .
#
# Not yet installed (add when the stage that needs them is reached):
#   xgboost>=2.0        -> model stage
#   neurokit2>=0.2.7    -> EDA/BVP feature stage, only if used
#   jupyter             -> notebooks
#   shap                -> only if feature-reliance analysis is kept
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
pyyaml>=6.0
joblib>=1.3
matplotlib>=3.8
pytest>=8.0
