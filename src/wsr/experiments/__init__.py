"""Experiment runners.

Planned modules: baseline.py, exp_a_label_selection.py, exp_b_policy_selection.py,
exp_c_random_audit.py, nurse_case_study.py.

Each runner must write: config snapshot, git commit, seed, split manifest,
frozen predictions, observation masks, metrics tables - see docs/experiment_log.md.
"""
